import React from 'react';
import { Box } from '@mui/material';

// ===== Main =====
const Home: React.FC = () => {
  return (
    <Box
      sx={{
        height: '100vh',
        width: '100%',
      }}
    >
    </Box>
  );
};

export default Home;
